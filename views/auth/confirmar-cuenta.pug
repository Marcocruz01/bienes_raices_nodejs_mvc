extends ../layout/index 

block contenido
    .py-10
        h1.text-4xl.my-10.font-extrabold.text-center Bienes     
            span.font-normal.text-gray-400 Raices
        h2.text-center.text-4xl.font-bold= pagina
        div(class="bg-white shadow py-8 px-4 rounded-lg max-w-md mx-auto mt-5")
            p(class= error ? 'bg-red-500' : 'bg-green-500').py-2.px-5.rounded-lg.w-full.text-white.font-bold.uppercase.text-center= mensaje

            if !error 
                a(href="/auth/login" class="text-center font-bold text-sm text-slate-600 mt-10 block bg-gray-200 rounded px-4 py-2 hover:bg-gray-300 hover:transition-all") Da click a este enlace para iniciar sesión 